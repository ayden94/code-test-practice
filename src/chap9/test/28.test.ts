import { solution } from '../28'

describe("", () => {
  test("이진 탐색 트리에서 찾을 수 있는지 확인하여true 또는 false를 담은 배열 result를 반환하는 함수 solution( )을 작성하세요.", () => {
    expect(solution(8, 4, 7)).toStrictEqual(3);
  })
})

